# Password-Hashing-and-Salting-using-python
This project demonstrates a basic password storage workflow using Python. The system generates a unique user identifier, creates a salt, applies SHA-256 hashing to the password, and derives a salted password value using PBKDF2-HMAC. The generated information is then organized into three in-memory tables for demonstration.
